query SchematicCollections($path: String!) {
  workspace(path: $path)
    @NgModule(module: "../feature-generate.module#FeatureGenerateModule") {
    schematicCollections {
      name
      schematics {
        name
        description
        collection
      }
    }
  }
}

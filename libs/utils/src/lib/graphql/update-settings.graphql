mutation UpdateSettings($data: String!) {
  updateSettings(data: $data) {
    canCollectData
    installNodeManually
    enableDetailedStatus
    isWsl
    useNvm
    channel
    recent {
      path
      name
      favorite
      pinnedProjectNames
    }
  }
}

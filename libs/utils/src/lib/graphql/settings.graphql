query Settings {
  settings {
    canCollectData
    installNodeManually
    enableDetailedStatus
    channel
    isConnectUser
    recent {
      path
      name
      favorite
      pinnedProjectNames
    }
  }
}

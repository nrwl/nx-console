query Settings {
  settings {
    isWsl
    useNvm
    canCollectData
    installNodeManually
    enableDetailedStatus
    channel
    disableAnimations
    isConnectUser
    recent {
      path
      name
      favorite
      pinnedProjectNames
    }
  }
}

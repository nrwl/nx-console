query Settings {
  settings {
    isWsl
    canCollectData
    installNodeManually
    enableDetailedStatus
    channel
    isConnectUser
    recent {
      path
      name
      favorite
    }
  }
}

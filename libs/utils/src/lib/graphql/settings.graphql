query Settings {
  settings {
    isWsl
    isWindows
    useNvm
    canCollectData
    installNodeManually
    enableDetailedStatus
    channel
    disableAnimations
    isConnectUser
    recent {
      path
      name
      favorite
      pinnedProjectNames
    }
  }
}

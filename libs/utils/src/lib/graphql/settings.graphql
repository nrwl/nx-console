query Settings {
  settings {
    isWsl
    useNvm
    canCollectData
    installNodeManually
    enableDetailedStatus
    channel
    isConnectUser
    recent {
      path
      name
      favorite
      pinnedProjectNames
    }
  }
}

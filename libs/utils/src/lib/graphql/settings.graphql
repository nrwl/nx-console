query Settings {
  settings {
    isWsl
    isWindows
    useNvm
    canCollectData
    installNodeManually
    enableDetailedStatus
    channel
    showRoutesSideNav
    disableAnimations
    isConnectUser
    recent {
      path
      name
      favorite
      pinnedProjectNames
    }
  }
}

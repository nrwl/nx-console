mutation NgNew($path: String!, $name: String!, $collection: String!) {
  ngNew(path: $path, name: $name, collection: $collection)
    @NgModule(module: "../feature-workspaces.module#FeatureWorkspacesModule") {
    id
  }
}

query Workspace($path: String!) {
  workspace(path: $path)
    @NgModule(module: "../feature-workspaces.module#FeatureWorkspacesModule") {
    name
    path
    dependencies {
      name
      version
    }
    projects {
      name
      root
      projectType
      architect {
        name
      }
    }
  }
}

import { vscodeProjectGraph } from './vscode-project-graph';

describe('vscodeProjectGraph', () => {
  it('should work', () => {
    expect(vscodeProjectGraph()).toEqual('vscode-project-graph');
  });
});

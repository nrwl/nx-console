import { vscodeNxCloudOnboarding } from './vscode-nx-cloud-onboarding';

describe('vscodeNxCloudOnboarding', () => {
  it('should work', () => {
    expect(vscodeNxCloudOnboarding()).toEqual('vscode-nx-cloud-onboarding');
  });
});

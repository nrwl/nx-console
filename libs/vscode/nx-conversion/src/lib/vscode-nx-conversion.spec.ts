import { vscodeNxConversion } from './vscode-nx-conversion';

describe('vscodeNxConversion', () => {
  it('should work', () => {
    expect(vscodeNxConversion()).toEqual('vscode-nx-conversion');
  });
});

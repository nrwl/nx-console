common-env: &common-env
  NX_VERBOSE_LOGGING: true
  NX_PLUGIN_NO_TIMEOUTS: true
common-init-steps: &common-init-steps
  - name: Checkout
    uses: 'nrwl/nx-cloud-workflows/v4/workflow-steps/checkout/main.yaml'
  - group-name: Restore Caches
    steps:
      - name: Setup Java 21 and IntelliJ UI dependencies
        script: |
          sudo apt update
          sudo apt install -y openjdk-21-jdk xvfb ffmpeg fonts-dejavu-core libasound2 libnss3 libxkbfile1 libx11-6 libxi6 libxtst6 libxrender1 libxrandr2 libglib2.0-0 libdbus-1-3 libgbm1
          sudo update-alternatives --set java /usr/lib/jvm/java-21-openjdk-amd64/bin/java
          echo "JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64" >> "$NX_CLOUD_ENV"
          java -version
      - name: Restore Node Modules Cache
        uses: 'nrwl/nx-cloud-workflows/v4/workflow-steps/cache/main.yaml'
        inputs:
          key: 'package-lock.json|yarn.lock|pnpm-lock.yaml'
          paths: 'node_modules'
          base-branch: 'main'
      - name: Restore Browser Binary Cache
        uses: 'nrwl/nx-cloud-workflows/v4/workflow-steps/cache/main.yaml'
        inputs:
          key: 'package-lock.json|yarn.lock|pnpm-lock.yaml|"browsers"'
          paths: |
            '../.cache/Cypress'
          base-branch: 'main'
  - group-name: Install Dependencies
    steps:
      - name: Install Node Modules
        uses: 'nrwl/nx-cloud-workflows/v4/workflow-steps/install-node-modules/main.yaml'
      - name: Generate workspace data
        script: npx nx show projects
  - name: Install Browsers
    uses: 'nrwl/nx-cloud-workflows/v4/workflow-steps/install-browsers/main.yaml'

launch-templates:
  linux-medium-plus-js:
    resource-class: 'docker_linux_amd64/medium+'
    image: 'ubuntu22.04-node20.19-v3'
    env: *common-env
    init-steps: *common-init-steps
  linux-large-plus-js:
    resource-class: 'docker_linux_amd64/large+'
    image: 'ubuntu22.04-node20.19-v3'
    no-output-timeout: '30m'
    env: *common-env
    init-steps: *common-init-steps
  # windows-medium-js:
  #   resource-class: 'windows/medium'
  #   image: 'windows-2022'
  #   init-steps:
  #     - name: Checkout
  #       uses: 'nrwl/nx-cloud-workflows/v4/workflow-steps/checkout/main.yaml'
  #     - name: Restore Node Modules Cache
  #       uses: 'nrwl/nx-cloud-workflows/v4/workflow-steps/cache/main.yaml'
  #       inputs:
  #         key: 'package-lock.json|yarn.lock|pnpm-lock.yaml'
  #         paths: 'node_modules'
  #         base-branch: 'main'
  #     - name: Restore Browser Binary Cache
  #       uses: 'nrwl/nx-cloud-workflows/v4/workflow-steps/cache/main.yaml'
  #       inputs:
  #         key: 'package-lock.json|yarn.lock|pnpm-lock.yaml|"browsers"'
  #         paths: |
  #           '../.cache/Cypress'
  #           '../.cache/ms-playwright'
  #         base-branch: 'main'
  #     - name: Install Node Modules
  #       uses: 'nrwl/nx-cloud-workflows/v4/workflow-steps/install-node-modules/main.yaml'
  #     - name: Install Browsers (if needed)
  #       uses: 'nrwl/nx-cloud-workflows/v4/workflow-steps/install-browsers/main.yaml'
  #     - name: Install cypress with --force
  #       script: npx cypress install --force

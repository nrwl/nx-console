name: CI Checks

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  main:
    name: Nx Cloud - Main Job
    uses: ./.github/workflows/run_checks_and_upload.yml

  agents:
    name: Nx Cloud - Agents
    uses: nrwl/ci/.github/workflows/nx-cloud-agents.yml@v0.6
    with:
      install-commands: 'yarn install --immutable'
      number-of-agents: 3

  agent-e2e:
    name: Nx Cloud - Agent E2E
    uses: nrwl/ci/.github/workflows/nx-cloud-agents.yml@v0.6
    with:
      environment-variables: |
        NX_RUN_GROUP=run-group-e2e-${{ github.run_id}}-${{ github.run_attempt }}
      install-commands: 'sudo apt-get install -y xvfb && yarn install --immutable'
      number-of-agents: 1

  # upload-artifacts:
  #   name: Upload E2E screenshots
  #   if: failure()
  #   runs-on: ubuntu-latest
  #   needs: main
  #   steps:
  #     - run: ls apps/vscode-e2e/
  #     - run: ls apps/vscode-e2e/.screenshots
  #     - uses: actions/checkout@v3
  #     - uses: actions/upload-artifact@v3
  #       with:
  #         name: e2e-screenshots
  #         path: '${{ github.workspace }}/apps/vscode-e2e/.screenshots/'
